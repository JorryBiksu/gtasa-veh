// pages/signing.js (or Signing.js if it's a page)
import React, { useState, useEffect } from 'react';
import Login from '@/components/Layout/auth/login';

const Signing = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default Signing;
