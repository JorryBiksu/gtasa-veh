// pages/signing.js (or Signing.js if it's a page)
import React from 'react';
import { UserInfoAction } from '@/components/Layout/userProfile';

const Profile = () => {
  return (
    <div>
      <UserInfoAction />
    </div>
  );
};

export default Profile;
